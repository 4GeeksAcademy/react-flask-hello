export const BackendURL = () => (
    <div className="text-center mt-5">
      <h1 className="text-danger">⚠️ BACKEND URL no configurado</h1>
      <p>Asegúrate de tener VITE_BACKEND_URL definido en tu archivo `.env`.</p>
    </div>
  );
  